[package]
license.workspace = true
publish.workspace = true
name = "rusty-websocket"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
rusty-web-crypto = { path = "../web-crypto" }
